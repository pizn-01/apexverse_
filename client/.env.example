# Environment variables for Vite (client-side)
# These will be embedded in the frontend build

# Backend API URL - leave empty for development (uses Vite proxy)
# For production, set this to your Railway/Render backend URL
# Example: VITE_API_URL=https://your-backend.railway.app
VITE_API_URL=
